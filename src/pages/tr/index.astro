---
import Layout from "../../layouts/Layout.astro";
import Home from "../../components/Home.astro";
import { SITE } from "../../site";

const lang = "tr";
---

<Layout title={SITE.name} description={`${SITE.name} — Portfolio.`} lang={lang}>
  <Home lang={lang} />
</Layout>